# Migration `20210109223846-create-algo-models`

This migration has been generated by Alex at 1/9/2021, 2:38:47 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "Difficulty" (
"id" SERIAL,
    "name" TEXT NOT NULL,
    "color" TEXT NOT NULL,
    "description" TEXT NOT NULL,

    PRIMARY KEY ("id")
)

CREATE TABLE "TestCase" (
"id" SERIAL,
    "problemId" INTEGER NOT NULL,
    "input" TEXT NOT NULL,
    "output" TEXT NOT NULL,

    PRIMARY KEY ("id")
)

CREATE TABLE "Problem" (
"id" SERIAL,
    "name" TEXT NOT NULL,
    "description" TEXT NOT NULL,
    "difficultyId" INTEGER NOT NULL,
    "hints" TEXT[],
    "sampleInput" TEXT NOT NULL,
    "sampleOutput" TEXT NOT NULL,

    PRIMARY KEY ("id")
)

ALTER TABLE "TestCase" ADD FOREIGN KEY("problemId")REFERENCES "Problem"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "Problem" ADD FOREIGN KEY("difficultyId")REFERENCES "Difficulty"("id") ON DELETE CASCADE ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20210109223846-create-algo-models
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,38 @@
+datasource DS {
+  // optionally set multiple providers
+  // example: provider = ["sqlite", "postgresql"]
+  provider = "postgresql"
+  url = "***"
+}
+
+generator client {
+  provider      = "prisma-client-js"
+  binaryTargets = "native"
+}
+
+model Difficulty {
+  id Int @id @default(autoincrement())
+  name String
+  color String
+  description String
+}
+
+model TestCase {
+  id Int @id @default(autoincrement())
+  problem Problem @relation(fields: [problemId], references: [id])
+  problemId Int
+  input String
+  output String
+}
+
+model Problem {
+  id Int @id @default(autoincrement())
+  name String
+  description String
+  difficulty Difficulty @relation(fields: [difficultyId], references: [id])
+  difficultyId Int
+  hints String[]
+  sampleInput String
+  sampleOutput String
+  testCases TestCase[]
+}
```


