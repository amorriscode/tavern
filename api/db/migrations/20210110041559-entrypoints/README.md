# Migration `20210110041559-entrypoints`

This migration has been generated by Alex at 1/9/2021, 8:15:59 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "Problem" ADD COLUMN     "entrypoints" JSONB
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20210110032941-init..20210110041559-entrypoints
--- datamodel.dml
+++ datamodel.dml
@@ -1,9 +1,9 @@
 datasource DS {
   // optionally set multiple providers
   // example: provider = ["sqlite", "postgresql"]
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider      = "prisma-client-js"
@@ -48,24 +48,26 @@
   sampleOutput   String
   testCases      TestCase[]
   scaffolds      Scaffold[]
   checksum       String?
+  entrypoints    Json?
 }
 model Guild {
-  id String @id @default(uuid())
-  name String
-  experience Int @default(0)
-  createdAt DateTime @default(now())
+  id          String        @id @default(uuid())
+  name        String
+  experience  Int           @default(0)
+  createdAt   DateTime      @default(now())
+  Transaction Transaction[]
 }
 // experience transactions
 model Transaction {
-  id String @id @default(uuid())
-  guild Guild? @relation(fields: [guildId], references: [id])
+  id      String   @id @default(uuid())
+  guild   Guild?   @relation(fields: [guildId], references: [id])
   guildId String?
-  delta Int
-  result Int // total experience after delta
-  time DateTime @default(now())
+  delta   Int
+  result  Int // total experience after delta
+  time    DateTime @default(now())
   // todo: user id
   // transactions can exist for both users + guilds
 }
```


